@using BeautyStudioSystem.Core.ViewModels
@model IEnumerable<ReservationViewModel>
@{
    ViewData["Title"] = "Employee Reservations";
}
<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3">@ViewBag.EmployeeName's Reservations</h1>
</div>

@if (ViewBag.Message != null)
{
        <div class="alert alert-info">@ViewBag.Message</div>
}

<div class="table-responsive">
    <table class="table table-striped table-hover align-middle">
        <thead class="table-success-header">
            <tr>
                <th scope="col">Client</th>
                <th scope="col">Service</th>
                <th scope="col">Date</th>
                <th scope="col">Start Time</th>
                <th scope="col">End Time</th>
            </tr>
        </thead>
        <tbody>
            @if (Model == null || !Model.Any())
            {
                    <tr>
                        <td colspan="5" class="text-center py-4">No reservations found.</td>
                    </tr>
            }
            else
            {
                foreach (var reservation in Model)
                {
                            <tr>
                                <td>@reservation.ClientName</td>
                                <td>@reservation.ServiceName</td>
                                <td>@reservation.Date</td>
                                <td>@reservation.StartTime</td>
                                <td>@reservation.EndTime</td>
                            </tr>
                }
            }
        </tbody>
    </table>
</div>
<a asp-action="AdminIndex" class="btn btn-secondary mt-3">Back to Employees</a>